# Private chart in directory : argocd/charts/dashboard

service:
  type: NodePort
  nodePort: 30001
rbac:
  enabled: true
ingress:
  enabled: false
metrics:
  enabled: true
app:
  scheduling:
    nodeSelector:
      kubernetes.io/hostname: minikube-m02
  tolerations:
    - key: "apps"
      operator: "Equal"
      value: "nginx"
      effect: "NoSchedule"
